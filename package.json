{
  "name": "slop-shield",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that covers likely AI-style filler using Jev",
  "scripts": { "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=20" }
}
